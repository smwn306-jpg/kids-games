# V25.6.1 Fix Audit

## Fixed from user test
- Removed invalid `pointerEvents` props from React Native `Image` components.
- Wrapped full-screen background Image layers in `View pointerEvents="none"`.
- Excluded `archive/**` backup files from TypeScript compilation.
- Added Expo web dependencies: `react-native-web@0.21.0`, `react-dom@19.1.0`, `@expo/metro-runtime@~6.1.0`.
- Enabled Expo Metro web bundler in `app.json`.
- Changed `ArtworkScreen` image fitting from `stretch` to `cover` to avoid distortion.

## Validation
Static source scan: no remaining `pointerEvents` prop directly on `<Image>` components.
A full dependency install/build must be run in the user's workspace after replacing the project files.
